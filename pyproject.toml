[project]
name = "aws-cloud-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rlop74", email = "119091421+rlop74@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.5",
    "python-dotenv>=1.2.3",
]

[project.scripts]
aws-cloud-lab = "aws_cloud_lab:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
